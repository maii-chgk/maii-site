<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ index_page.Title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/uikit@3.6.16/dist/css/uikit.min.css" />
  <link href="https://fonts.googleapis.com/css2?family=Rubik&family=Roboto+Slab&display=swap" rel="stylesheet"> 
  <link rel="stylesheet" href="/static/main.css" />

  <meta property="og:title" content="{{ index_page.Title }}" />
  <meta property="og:locale" content="ru_RU" />
  {% if env.HOST %}
    <meta property="og:url" content="{{ env.HOST }}" />
    <meta property="og:image" content="{{ env.HOST }}/static/favicon-512.png" />
  {% endif %}

  <link rel="icon" href="/static/favicon.ico"><!-- 32×32 -->
  <link rel="icon" href="/static/favicon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/static/apple-touch-icon.png"><!-- 180×180 -->
  <link rel="manifest" href="/static/manifest.webmanifest">
</head>
<body>
  <section class="uk-section uk-section-primary uk-flex uk-flex-middle uk-text-center section-full">
    <div class="uk-container">
      <h1 class="uk-margin-large-bottom">{{ index_page.Title }}</h1>
      <div class="uk-text-lead">
        {# NB: do not split/indent the following line and other MD blocks #}
        {% markdown %}{{ index_page.Lead | safe }}{% endmarkdown %}
      </div>
    </div>
  </section>

  <section class="uk-section uk-section-secondary">
    <div class="uk-container">
      {% markdown %}{{ index_page.Main | safe }}{% endmarkdown %}
    </div>
  </section>

  <section class="uk-section uk-section-muted">
    <div class="uk-container">
      <h2 class="">{{ index_page.FeaturesTitle }}</h1>
      <div class="uk-grid uk-grid-match uk-child-width-1-1 uk-child-width-1-2@s uk-child-width-1-3@m uk-text-center uk-flex-center">
        {% for feature in index_page.Features %}
          <div class="uk-card uk-card-body">
            <h3 class="uk-card-title">{{ feature.Title }}</h3>
            <div>
              {% markdown %}{{ feature.Description | safe }}{% endmarkdown %}
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </section>

  <section class="uk-section uk-section-default">
    <div class="uk-container">
      {% markdown %}{{ index_page.Main2 | safe }}{% endmarkdown %}
    </div>
  </section>

  <footer class="uk-section uk-section-muted uk-section-small">
    <div class="uk-container uk-text-small uk-text-muted">
      {% markdown %}{{ index_page.Footer | safe }}{% endmarkdown %}
    </div>
  </footer>

  <script>
    (function(){
      [...document.querySelectorAll('a')].forEach(a => {
        if ((a.getAttribute('href') || '').match(/^https?:\/\//)) {
          a.setAttribute('target', '_blank');
        }
      });
    }())
  </script>
</body>
</html>